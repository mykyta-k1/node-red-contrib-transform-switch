{
  "name": "node-red-contrib-transform-switch",
  "version": "1.0.0",
  "description": "Node-RED node that combines data transformation and flow routing. Applies multiple operations to a single input and routes results to different output ports.",
  "main": "lib/transform-switch.js",
  "scripts": {
    "test": "mocha test/*_spec.js",
    "lint": "eslint ."
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mykyta-k1/node-red-contrib-transform-switch"
  },
  "keywords": [
    "node-red",
    "switch",
    "change",
    "transform",
    "route",
    "map",
    "flow-control",
    "data-processing"
  ],
  "author": "Mykyta",
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@types/node-red": "^1.3.0",
    "eslint": "^8.0.0",
    "mocha": "^10.0.0",
    "node-red-node-test-helper": "^0.3.0"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "node-red": {
    "version": ">=3.0.0",
    "nodes": {
      "transform-switch": "lib/transform-switch.js"
    }
  },
  "browserslist": [
    "last 2 versions",
    "not dead"
  ]
}